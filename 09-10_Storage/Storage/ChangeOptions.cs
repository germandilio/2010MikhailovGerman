namespace Storage
{   
    /// <summary>
    /// Перечисления опций для формы (сбор информации от пользователя).
    /// </summary>
    enum ChangeOptions
    { 
        AddNode,
        ChangeNode,
        SaveFile,
        RestoreFile,
        NumberToCSV
    }
}
